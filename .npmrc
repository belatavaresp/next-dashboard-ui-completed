# Always resolve from public npm. Without this, a machine configured against a
# private proxy bakes unreachable "resolved" URLs into package-lock.json, and the
# Vercel build fails with 403 on every tarball.
registry=https://registry.npmjs.org/
